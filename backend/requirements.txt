# Production dependencies — all versions pinned for reproducibility.
#
# [SECURITY] python-jose 3.3.0 has a known algorithm-confusion CVE
# (CVE-2024-33664 / GHSA-m85q-8fjx-8hpw). Mitigation applied in code:
# decode_access_token() passes algorithms=["HS256"] explicitly, which
# prevents the 'none' algorithm attack. Upgrading to PyJWT is tracked as
# a future migration task. Pin to 3.3.0 for now — do NOT downgrade.
#
# [SECURITY] passlib 1.7.4 + bcrypt 4.0.1: bcrypt is used for password hashing.
# bcrypt 4.x requires an explicit version pin to avoid the 4.1+ passlib
# compatibility break (passlib calls a deprecated internal API).

fastapi==0.111.0
uvicorn[standard]==0.30.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
slowapi==0.1.9
sqlalchemy==2.0.30
pydantic-settings==2.3.1
python-dotenv==1.0.1
email-validator==2.1.2
numpy==1.26.4
scikit-learn==1.5.0
httpx==0.27.0
pytest==8.2.2
pytest-cov==5.0.0
anyio==4.4.0
